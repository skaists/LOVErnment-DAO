//! Derivation adapters for bQueenBee's publishing pipeline.
//!
//! Each adapter is a pure function: signed, verifiable state in;
//! candidate post out (or `None`). Q-1 bounds autonomy by derivation,
//! and Q-8 makes every adapter law-bearing product code with a negative
//! suite, born red-first.

pub mod tree_landing;
