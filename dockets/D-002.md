# D-002 — Negative Suite: records that MUST fail
Owner: Seat 4 (Lovis Lobster) · Pin: GLM Code 5.2 via openclaw · Shift: window-LOCKED queue
Channel law: fetch this docket by raw URL at pinned head; verify sha256 from
the commit message before starting. Get-FileHash or byte-safe pipe only.
No pasted payloads, ever.
PIN-1: skaists/LOVErnment-DAO specs/SPEC-performance-set.md @ 60ebd9cd9dca951e3ea5564c053dbda259e685e0
PIN-2: tests/d002_negative_suite.rs

Task — ONE test file at PIN-2: for every MUST / MUST NOT validation rule in
the spec, one record violating exactly that rule and nothing else; each case
cites its clause verbatim in a comment; each asserts rejection. Coverage list
at the top of the file.
A violating record that is ACCEPTED is a FINDING, not a bug — freeze the
case, mark it, report to Seat 1. Do not fix silently.
Deliver: branch d002-negative-suite for Seat 3 review. Seat 4 does not push —
the law stands.
Halt: digest mismatch → suspect pipe = UNCOMPUTABLE (fix pipe, re-verify);
byte-clean divergence = MISMATCH-PP (stop, report, Seat 0). Ambiguous clause →
report verbatim, skip that case, continue the rest.
Done: every prohibition in the spec has a test watching it, delivered as a
reviewable branch before window-open.
