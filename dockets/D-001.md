# D-001 — First Compiler Contact: performance-set putRecord
Owner: Seat 3 · Pin: per ORDERS-1 §1 · Phase: one · Files: one
PIN-1: skaists/LOVErnment-DAO specs/SPEC-performance-set.md @ 60ebd9cd9dca951e3ea5564c053dbda259e685e0
PIN-2: tests/d001_performance_putrecord.rs
PIN-3: LIVE PDS, self-cleaning (founder-approved 2026-07-11)

Source of truth: the spec at PIN-1 — as committed, not as remembered.
All field and reference handling exactly as the committed spec defines
(SET-8: eventUri is enrichment, never dependency; venue is load-bearing).
The spec overrides all prior memory, including Seat 1's — twice proven.

Task — ONE test file at PIN-2 that:
1. constructs the minimal valid record of social.skaists.alpha.performance.set;
2. putRecord to the live PDS under the skaists DID (env contract below);
3. getRecord round-trip by returned at-uri; assert equivalence per the spec;
4. asserts SET-4 exactly as written, clause quoted verbatim in a comment;
5. deleteRecord — self-cleaning; commit + test transcript prove existence.

Env contract: SKAISTS_PDS_URL, SKAISTS_HANDLE, SKAISTS_APP_PASSWORD —
env-only, never committed, never logged. Missing env → test reports
SKIPPED-ENV and the run is NOT green; a skipped D-001 is not the whistle.

Acceptance: canonical test command green on this file, steps 1–5; max one new
client dependency with one-line justification; commit on green with
sha256(test file) in message (checkpoint law).
Halt: SET-4 unassertable as written → stop, report the clause verbatim to
Seat 1. Any lexicon↔spec divergence → same. Never patch the spec.
Done: a record existed in the world and the committed test proves it.
The whistle.
